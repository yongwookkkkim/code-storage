
        int a, b, c, max;
        a = Integer.parseInt(args[0]);
        b = Integer.parseInt(args[1]);
        c = Integer.parseInt(args[2]);
        
        if (a >= b) max = a;
        else max = b;
        if(max<c) max = c;

        if (max > a+b+c - max) System.out.println("yes");
        else System.out.println("no");
        
        a = Integer.parseInt(args[0]);
        -> the first argument, which will be a string, should be converted to an int and stored in a
        args = Argument String
        main method: 
        실행(전달인자실행) -> $filedir(no extension) $전달인자묻기
        전달인자=전달인수
        3.0d -> 1.1 * 2^1
        0 10,000,000,000 1000.... (52bits in total)
        double -> int 보단 int-> double이 더 유리하다.
        묵시적 -> 알아서 소프트웨어가 형변환
        명시적 -> 프로그래머가 지시
        promotion and demotion: 
        byte -> short -> int -> long -> float -> double
        implicit type casting is always a promotion
        explicitly cast when you want a demotion
        cast 연산자()
        Boolean cannot be type casted

        double a;
        a = 6;
        works because its a promotion

        int a;
        a = 6.8;
        does not automatically work (an error) because data can be lost
        (int)3.4d -> 3